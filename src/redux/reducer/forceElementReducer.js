const initState = {
    
}
export const forceElementReducer = (state = initState, action) => {
    switch (action.type) {
        // case 'BACKGROUND_CLICK':
        //     if (state.indexClick > 0) {
        //         return {
        //             ...state,
        //             indexClick: state.indexClick - 1
        //         };
        //     }
        //     return state
       
        default:
            return state
    }
}
